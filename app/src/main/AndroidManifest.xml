<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.trackyourexpense">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TrackYourExpense">
        <activity
            android:name=".modules.transaction.TransactionActivity"
            android:exported="true" />
        <activity
            android:name=".modules.bottomnavigation.BottomNavigationView"
            android:exported="true" />
        <activity
            android:name=".modules.onboarding.OnboardingActivity"
            android:exported="true" />
        <activity
            android:name=".modules.splash.SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".modules.signup.SignupActivity"
            android:exported="true" />
        <activity
            android:name=".modules.signin.SignInActivity"
            android:exported="true" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>